---
phase: 02-workspace-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - crates/core/src/workspace.rs
  - crates/core/src/repo.rs
  - crates/core/src/id.rs
  - apps/cli/src/main.rs
autonomous: true
requirements: [WORK-01, WORK-02, WORK-04]

must_haves:
  truths:
    - "User can run `kmd workspace create <name> --repo <ref>` and a workspace is created and persisted"
    - "User can run `kmd workspace list` and see active workspaces in table format"
    - "User can run `kmd workspace show/delete/archive/activate` and the workspace state changes correctly"
    - "Workspaces persist in state.json and survive app restart"
    - "Old state.json files without a workspaces key load cleanly with empty workspace vec"
    - "Smart repo resolver accepts name, path, or ID and produces a clear error on failure"
  artifacts:
    - path: "crates/core/src/workspace.rs"
      provides: "Workspace struct and CRUD methods"
      exports: ["Workspace", "create_workspace", "list_workspaces", "resolve_repo"]
    - path: "crates/core/src/repo.rs"
      provides: "RepoEntry struct extracted from lib.rs"
      exports: ["RepoEntry"]
    - path: "crates/core/src/id.rs"
      provides: "Shared generate_id helper"
      exports: ["generate_id"]
    - path: "crates/core/src/lib.rs"
      provides: "AppState with workspaces vec, re-exports"
      contains: "pub workspaces: Vec<Workspace>"
    - path: "apps/cli/src/main.rs"
      provides: "Workspace CLI subcommands"
      contains: "WorkspaceAction"
  key_links:
    - from: "crates/core/src/workspace.rs"
      to: "crates/core/src/lib.rs"
      via: "AppState workspace methods"
      pattern: "impl AppState.*workspace"
    - from: "apps/cli/src/main.rs"
      to: "crates/core/src/lib.rs"
      via: "AppState workspace CRUD calls"
      pattern: "state\\.(create_workspace|list_workspaces|archive_workspace|activate_workspace|delete_workspace|show_workspace|resolve_repo)"
    - from: "crates/core/src/lib.rs"
      to: "state.json"
      via: "serde with #[serde(default)] on workspaces"
      pattern: "serde\\(default\\)"
---

<objective>
Build the Workspace domain model in core crate with full CRUD operations, smart repo resolver, and CLI subcommands for all 6 workspace actions.

Purpose: Establishes the workspace data model and persistence that the TUI (Plan 02) will consume. Provides the complete CLI surface for workspace management.
Output: Workspace struct, AppState with workspaces persistence, CLI commands (create/list/show/delete/archive/activate), unit tests.
</objective>

<execution_context>
@/Users/csaba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/csaba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-model/02-CONTEXT.md
@.planning/phases/02-workspace-model/02-RESEARCH.md
@.planning/phases/02-workspace-model/02-VALIDATION.md

<interfaces>
<!-- Current core exports used by CLI and TUI -->

From crates/core/src/lib.rs:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RepoEntry {
    pub id: String,
    pub name: String,
    pub path: String,
}

#[derive(Debug, Default, Serialize, Deserialize)]
pub struct AppState {
    pub repos: Vec<RepoEntry>,
}

impl AppState {
    pub fn load_from(base: &Path) -> anyhow::Result<Self>;
    pub fn save_to(&self, base: &Path) -> anyhow::Result<()>;
    pub fn load() -> anyhow::Result<Self>;
    pub fn save(&self) -> anyhow::Result<()>;
    pub fn add_repo_with_base(&mut self, path: &str, base: &Path) -> anyhow::Result<RepoEntry>;
    pub fn add_repo(&mut self, path: &str) -> anyhow::Result<RepoEntry>;
}

pub fn generate_id() -> String;
pub fn run_git_status(repo_path: &str) -> anyhow::Result<String>;
```

From apps/cli/src/main.rs:
```rust
#[derive(Subcommand)]
enum Commands {
    Repo { #[command(subcommand)] action: RepoAction },
}
#[derive(Subcommand)]
enum RepoAction {
    Add { path: String },
    List,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core workspace model with CRUD, resolver, and persistence</name>
  <files>Cargo.toml, crates/core/Cargo.toml, crates/core/src/lib.rs, crates/core/src/workspace.rs, crates/core/src/repo.rs, crates/core/src/id.rs</files>
  <behavior>
    - create_workspace: creates workspace with auto-generated name from repo name when name is None
    - create_workspace: creates workspace with explicit name when provided
    - create_workspace: errors with "workspace already exists: {name}" for duplicate name
    - create_workspace: errors when repo_ref does not match any repo
    - create_workspace: sets working_dir to repo.path, active to true, created_at to unix seconds
    - resolve_repo: resolves by name match
    - resolve_repo: resolves by path match (canonicalized)
    - resolve_repo: resolves by ID match
    - resolve_repo: path-first if input contains '/'
    - resolve_repo: error message includes "Checked: name, path, id" on failure
    - list_workspaces (active only): returns only active workspaces
    - list_workspaces (all): returns active and archived
    - list_workspaces (by repo): filters by repo_id
    - archive_workspace: sets active=false by name lookup
    - activate_workspace: sets active=true by name lookup
    - delete_workspace: removes workspace from vec by name
    - show_workspace: finds workspace by name, returns reference or error
    - roundtrip: save state with workspaces, load it back, workspaces preserved
    - backward_compat: JSON with only "repos" key deserializes cleanly (workspaces defaults to empty vec)
  </behavior>
  <action>
    Split crates/core/src/lib.rs into modules per research recommendation:

    1. Create `crates/core/src/id.rs`: Move `generate_id()` here. Make it `pub`.

    2. Create `crates/core/src/repo.rs`: Move `RepoEntry` struct and `run_git_status()` here. Keep derives.

    3. Create `crates/core/src/workspace.rs`: Define `Workspace` struct with fields per CONTEXT.md decisions:
       - `id: String`, `name: String`, `repo_id: String`, `working_dir: String`, `active: bool`, `created_at: u64`
       - All fields pub, derive `Debug, Clone, Serialize, Deserialize`

    4. Update `crates/core/src/lib.rs`:
       - Declare modules: `pub mod id; pub mod repo; pub mod workspace;`
       - Re-export everything: `pub use id::generate_id; pub use repo::{RepoEntry, run_git_status}; pub use workspace::Workspace;`
       - Keep `AppState` in lib.rs with `pub repos: Vec<RepoEntry>` and `#[serde(default)] pub workspaces: Vec<Workspace>`
       - Keep load/save/load_from/save_to methods on AppState
       - Keep add_repo/add_repo_with_base on AppState
       - Add workspace methods on AppState:
         - `resolve_repo(&self, reference: &str) -> Result<&RepoEntry>` -- path check (contains '/') with canonicalize, then name, then ID; error with guidance message
         - `create_workspace_with_base(&mut self, name: Option<&str>, repo_ref: &str, base: &Path) -> Result<Workspace>` -- resolve repo, derive name if None, check uniqueness, build Workspace, push, save_to(base)
         - `create_workspace(&mut self, name: Option<&str>, repo_ref: &str) -> Result<Workspace>` -- delegates to with_base
         - `list_workspaces(&self, all: bool, repo_ref: Option<&str>) -> Result<Vec<&Workspace>>` -- filter by active (unless all), filter by repo_id if repo_ref provided (resolve first)
         - `show_workspace(&self, name: &str) -> Result<&Workspace>` -- find by name or bail
         - `delete_workspace_with_base(&mut self, name: &str, base: &Path) -> Result<Workspace>` -- remove by name, save_to(base), return removed
         - `delete_workspace(&mut self, name: &str) -> Result<Workspace>` -- delegates to with_base
         - `archive_workspace_with_base(&mut self, name: &str, base: &Path) -> Result<()>` -- find mut, set active=false, save
         - `archive_workspace(&mut self, name: &str) -> Result<()>` -- delegates
         - `activate_workspace_with_base(&mut self, name: &str, base: &Path) -> Result<()>` -- find mut, set active=true, save
         - `activate_workspace(&mut self, name: &str) -> Result<()>` -- delegates
       - Move existing tests to appropriate modules; repo tests to repo.rs or keep in lib.rs

    5. Add `chrono = "0.4"` to `[workspace.dependencies]` in root Cargo.toml and `chrono.workspace = true` to `crates/core/Cargo.toml` for timestamp formatting.

    6. Add a `format_timestamp(unix_secs: u64) -> String` helper in workspace.rs using chrono::Local for "YYYY-MM-DD HH:MM" format.

    TDD: Write failing tests first in workspace.rs `#[cfg(test)] mod tests` block covering all behaviors listed above. Then implement to make them pass. Use tempfile for filesystem isolation following the established base-path injection pattern.

    IMPORTANT: Re-exports from lib.rs must maintain backward compatibility -- `use kommand0_core::{AppState, RepoEntry, run_git_status}` must still work in CLI and TUI without import changes.
  </action>
  <verify>
    <automated>cd /Users/csaba/conductor/workspaces/kommand0/mumbai && cargo test -p kommand0-core 2>&1</automated>
  </verify>
  <done>Workspace struct defined with all 6 fields. AppState has #[serde(default)] workspaces vec. All CRUD methods implemented. Smart repo resolver works for name/path/ID. 15+ unit tests pass including backward compatibility. Core module split into repo.rs, workspace.rs, id.rs with re-exports from lib.rs. chrono dependency added.</done>
</task>

<task type="auto">
  <name>Task 2: CLI workspace subcommands</name>
  <files>apps/cli/src/main.rs</files>
  <action>
    Add the full Workspace CLI subcommand surface to apps/cli/src/main.rs:

    1. Add `Workspace` variant to `Commands` enum:
       ```rust
       Workspace {
           #[command(subcommand)]
           action: WorkspaceAction,
       },
       ```

    2. Define `WorkspaceAction` enum with 6 variants per CONTEXT.md:
       - `Create { name: Option<String>, #[arg(long)] repo: String }` -- name is optional positional, repo is required flag
       - `List { #[arg(long)] all: bool, #[arg(long)] repo: Option<String> }`
       - `Show { name: String }` -- positional
       - `Delete { name: String, #[arg(long)] force: bool }` -- positional name, optional force flag
       - `Archive { name: String }` -- positional
       - `Activate { name: String }` -- positional

    3. Add match arm in main() for `Commands::Workspace { action }`:

       - **Create**: Call `state.create_workspace(name.as_deref(), &repo)`. Print "Created workspace: {name} (repo: {repo_name})". Look up repo name from state for the print.
       - **List**: Call `state.list_workspaces(all, repo.as_deref())`. Print table with headers "ID NAME REPO STATUS" using manual format! with column padding (12, 20, 20, 10). If empty, print "No workspaces found." Handle repo_name lookup gracefully (show "(unknown)" for missing repos).
       - **Show**: Call `state.show_workspace(&name)`. Print all fields: Name, ID, Repo (name + path), Dir, Status (active/archived), Created (formatted timestamp using `workspace::format_timestamp`).
       - **Delete**: Implement confirmation prompt: if `!force`, check `std::io::stdin().is_terminal()`. If not a TTY without force, eprintln error and exit 1. If TTY, print "Delete workspace '{name}'? [y/N] ", flush stdout, read line, proceed only on "y"/"Y". Call `state.delete_workspace(&name)`. Print "Deleted workspace: {name} (repo: {repo_name})".
       - **Archive**: Call `state.archive_workspace(&name)`. Print "Archived workspace: {name}".
       - **Activate**: Call `state.activate_workspace(&name)`. Print "Activated workspace: {name}".

    4. Error handling: All errors propagate via anyhow::Result from core methods. Errors print to stderr (anyhow default). The core methods already produce terse error messages (e.g., "workspace already exists: foo").

    5. Add `use std::io::{IsTerminal, Write};` for TTY detection and stdout flush.
    6. Add `use kommand0_core::workspace::format_timestamp;` for show command.
  </action>
  <verify>
    <automated>cd /Users/csaba/conductor/workspaces/kommand0/mumbai && cargo build -p kommand0-cli 2>&1 && cargo test --workspace 2>&1</automated>
  </verify>
  <done>All 6 CLI commands compile and match the spec: create (with auto-name and explicit name), list (with --all and --repo filters), show, delete (with --force and TTY check), archive, activate. `kmd workspace --help` shows all subcommands. Full workspace test suite passes.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes (all core unit tests + no compile errors in CLI/TUI)
- `cargo build --workspace` compiles clean
- `kmd workspace create test-ws --repo <path>` creates a workspace and persists to state.json
- `kmd workspace list` shows the workspace in table format
- `kmd workspace show test-ws` displays all workspace fields
- `kmd workspace archive test-ws` sets inactive
- `kmd workspace activate test-ws` sets active again
- `kmd workspace delete test-ws --force` removes it
- Old state.json files (without workspaces key) load without error
</verification>

<success_criteria>
- Workspace CRUD fully functional via CLI with all 6 commands
- Smart repo resolver handles name, path, and ID inputs
- State persistence backward-compatible (serde default on workspaces)
- 15+ unit tests green covering all workspace behaviors
- Core module split cleanly with re-exports preserving import compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-model/02-01-SUMMARY.md`
</output>

---
phase: 01-stabilization-and-async-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - README.md
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-03
  - STAB-04
  - STAB-05

must_haves:
  truths:
    - "cargo test -p kommand0-core passes with tests for load/save roundtrip, add_repo validation, and git status edge cases"
    - "AppState can be tested in isolation using load_from/save_to with a temp directory"
    - "run_git_status rejects nonexistent paths, non-directories, and non-git directories with clear errors"
    - "README contains accurate build, run, and test instructions for a fresh clone"
    - "Naming across core/cli/tui is consistent with no stale or misleading identifiers"
  artifacts:
    - path: "crates/core/src/lib.rs"
      provides: "Testable AppState with load_from/save_to + unit tests + git status edge case handling"
      contains: "fn load_from"
    - path: "crates/core/Cargo.toml"
      provides: "tempfile dev-dependency for test isolation"
      contains: "tempfile"
    - path: "Cargo.toml"
      provides: "Workspace deps updated with tempfile, futures, crossterm event-stream feature"
      contains: "event-stream"
    - path: "README.md"
      provides: "Accurate build/run/test instructions"
      contains: "cargo test --workspace"
  key_links:
    - from: "crates/core/src/lib.rs (tests)"
      to: "AppState::load_from / save_to"
      via: "tempfile::TempDir passed as base path"
      pattern: "TempDir::new.*load_from"
    - from: "crates/core/src/lib.rs (tests)"
      to: "run_git_status"
      via: "edge case assertions on non-git paths"
      pattern: "run_git_status.*is_err"
---

<objective>
Core testability, unit tests, naming review, edge case hardening, and README update.

Purpose: Make the core crate testable and tested before the async migration in Plan 02 touches cargo configuration and TUI code. Establishes a test baseline so Plan 02 can verify nothing breaks.
Output: Tested core crate with load_from/save_to, git status edge cases, workspace dependency updates (including futures/crossterm event-stream for Plan 02), and accurate README.
</objective>

<execution_context>
@/Users/csaba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/csaba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilization-and-async-foundation/01-CONTEXT.md
@.planning/phases/01-stabilization-and-async-foundation/01-RESEARCH.md

<interfaces>
<!-- Current core crate API that tests will exercise -->
From crates/core/src/lib.rs:
```rust
pub struct RepoEntry { pub id: String, pub name: String, pub path: String }
pub struct AppState { pub repos: Vec<RepoEntry> }

impl AppState {
    const STATE_DIR: &str = ".kommand0-dev";
    const STATE_FILE: &str = "state.json";
    pub fn load() -> anyhow::Result<Self>;
    pub fn save(&self) -> anyhow::Result<()>;
    pub fn add_repo(&mut self, path: &str) -> anyhow::Result<RepoEntry>;
}

pub fn run_git_status(repo_path: &str) -> anyhow::Result<String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make AppState testable and add unit tests with edge case hardening</name>
  <files>Cargo.toml, crates/core/Cargo.toml, crates/core/src/lib.rs</files>
  <behavior>
    - Test: load_from returns default AppState when no state file exists in temp dir
    - Test: save_to + load_from roundtrip preserves repos (name, path, id)
    - Test: add_repo rejects nonexistent path with error
    - Test: add_repo rejects duplicate path with error
    - Test: add_repo canonicalizes path and derives name from directory
    - Test: run_git_status errors on nonexistent path
    - Test: run_git_status errors on path that is a file, not a directory
    - Test: run_git_status errors on non-git directory (temp dir with no .git)
  </behavior>
  <action>
    1. Update workspace Cargo.toml: add `tempfile = "3"` and `futures = "0.3"` to `[workspace.dependencies]`. Update crossterm to `crossterm = { version = "0.28", features = ["event-stream"] }`. These deps are needed across Plan 01 (tempfile) and Plan 02 (futures, event-stream).

    2. Update crates/core/Cargo.toml: add `[dev-dependencies]` section with `tempfile.workspace = true`.

    3. In crates/core/src/lib.rs, add `load_from(base: &Path)` and `save_to(&self, base: &Path)` methods to AppState:
       - `load_from` reads from `base.join(STATE_FILE)`, returns default if file missing
       - `save_to` creates `base` directory if needed, writes JSON to `base.join(STATE_FILE)`
       - Refactor existing `load()` to delegate to `load_from(Self::state_dir())` (use `state_dir().as_path()`)
       - Refactor existing `save()` to delegate to `save_to(Self::state_dir())` (use `state_dir().as_path()`)

    4. Also add `add_repo_with_base(&mut self, path: &str, base: &Path)` that saves to a custom base path (so tests don't write to `.kommand0-dev/`). Refactor `add_repo` to delegate to `add_repo_with_base` with the default state_dir.

    5. Add pre-validation to `run_git_status`: before spawning git, check that the path exists (`Path::new(repo_path).exists()`) and is a directory (`Path::new(repo_path).is_dir()`). Bail with clear messages: "path does not exist: {repo_path}" or "path is not a directory: {repo_path}".

    6. Review all pub items, comments, and doc strings across core/cli/tui for stale or misleading identifiers. The research and context confirm naming is already consistent, so this is a quick scan -- fix any stale comments found (STAB-01, STAB-02).

    7. Add `#[cfg(test)] mod tests` at the bottom of lib.rs with all tests from the behavior list above. Use `tempfile::TempDir` for filesystem isolation. Tests are synchronous (no `#[tokio::test]`).

    TDD: Write tests first (RED), then implement load_from/save_to/add_repo_with_base/run_git_status validation (GREEN), then clean up (REFACTOR).
  </action>
  <verify>
    <automated>cd /Users/csaba/conductor/workspaces/kommand0/minnetonka-v1 && cargo test -p kommand0-core -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>All 8+ unit tests pass. AppState has load_from/save_to methods. run_git_status rejects bad paths before spawning git. Existing load()/save()/add_repo() still work (delegate to new methods). No stale naming found or fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Update README with accurate instructions</name>
  <files>README.md</files>
  <action>
    Update README.md with accurate build/run/test instructions per CONTEXT.md decisions:

    - Prerequisites: Rust toolchain (edition 2024), git on PATH, macOS
    - Build: `cargo build --workspace`
    - Run CLI: `cargo run -p kommand0-cli -- repo add <path>` and `cargo run -p kommand0-cli -- repo list`
    - Run TUI: `cargo run -p kommand0-tui`
    - Test: `cargo test --workspace`
    - Keep workspace structure section
    - Keep keybinding section
    - Keep it short -- dev tool, not a product landing page
    - Add note about state file location (.kommand0-dev/state.json)
  </action>
  <verify>
    <automated>cd /Users/csaba/conductor/workspaces/kommand0/minnetonka-v1 && cargo build --workspace 2>&1 | tail -5 && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>README contains Prerequisites, Build, CLI usage, TUI usage, Test command, workspace structure, and keybindings. All commands listed in README actually work when executed.</done>
</task>

</tasks>

<verification>
- `cargo test -p kommand0-core` passes all unit tests
- `cargo build --workspace` succeeds (verifies dependency changes are valid)
- `cargo test --workspace` passes (full workspace still compiles and tests pass)
</verification>

<success_criteria>
- 8+ unit tests in kommand0-core covering AppState persistence and run_git_status edge cases
- AppState has load_from/save_to public methods accepting a base path
- run_git_status validates path existence and directory-ness before spawning git
- Workspace Cargo.toml has tempfile, futures, and crossterm event-stream feature
- README has accurate build/run/test instructions matching actual cargo commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-and-async-foundation/01-01-SUMMARY.md`
</output>

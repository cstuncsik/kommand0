# kommand0

Keyboard-first local orchestrator for parallel coding sessions.

## Workspace Structure

- `apps/cli` - Command-line interface (clap)
- `apps/tui` - Terminal UI (ratatui + crossterm)
- `crates/core` - Shared core library (tokio, serde, tracing)
